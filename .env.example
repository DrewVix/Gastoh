# Ruta a la base de datos SQLite (en Docker se sobreescribe via docker-compose)
DATABASE_URL="file:./prisma/data/gastoh.db"

# Secreto para las sesiones — genera uno con: openssl rand -hex 32
SESSION_SECRET="cambia-esto-por-una-cadena-aleatoria-de-al-menos-32-caracteres"
